// Empty stub of the `server-only` package, aliased in tsconfig.harness.json so the
// server-only engine modules can be unit-run under tsx (they never touch a real
// request; the harness passes a service-role client explicitly).
export {};
